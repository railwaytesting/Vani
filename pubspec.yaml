name: vani
description: "Vani ISL – Indian Sign Language Translation App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.11.0+2
  cupertino_icons: ^1.0.8

  # Localization (intl auto-managed by Flutter)
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
